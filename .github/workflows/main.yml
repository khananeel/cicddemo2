name: hello world workflow
on: push
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Hello Step
        run: |
          echo "Hello to Git Hub actions"
          docker ps
          ls -ltra

    
